export interface AcademicSession {
  id: number;
  name: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}

export interface Semester {
  id: number;
  name: string;
  createdAt: string;
  updatedAt: string;
}